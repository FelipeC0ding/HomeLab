apiVersion: v1
kind: Service
metadata:
  name: jellyfin-svc
  namespace: media
spec:
  type: LoadBalancer
  selector:
    app: jellyfin
  ports:
    - protocol: TCP
      port: 8096
      targetport: 8096
